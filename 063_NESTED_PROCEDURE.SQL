DECLARE
  PROCEDURE SHOW_NAME
  IS
    WNAME EMPLOYEES.LAST_NAME%TYPE;
  BEGIN
    SELECT LAST_NAME
    INTO WNAME
    FROM EMPLOYEES
    WHERE EMPLOYEE_ID = 101;
    PRINT('ISMI: ' || WNAME);
  END;
BEGIN
  SHOW_NAME;
END;
