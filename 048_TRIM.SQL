DECLARE
  TYPE VARR_TYPE
  IS
  VARRAY(10) OF INTEGER;

  VAR1 VARR_TYPE := VARR_TYPE(10, 78, 65, 22, 3, 7);
BEGIN
  PRINT('');

  FOR I IN 1..VAR1.COUNT
  LOOP
    PRINT('DEGER: ' || VAR1(I));
  END LOOP;

  VAR1.TRIM; --<< DIZININ SON ELEMANI SILER.

  PRINT('TRIM KULLANILDI SON ELEMAN SILINDI');
  FOR I IN 1..VAR1.COUNT
  LOOP
    PRINT('DEGER: ' || VAR1(I));
  END LOOP;

  VAR1.TRIM(2);
  PRINT('TRIM KULLANILDI 2. ELEMAN SILINDI');
  FOR I IN 1..VAR1.COUNT
  LOOP
    PRINT('DEGER: ' || VAR1(I));
  END LOOP;

  VAR1.DELETE;
  PRINT('DELETE KULLANILDI TÜM ELEMANLAR SILINDI');
  FOR I IN 1..6 --<<DONGUYE GIRMEDIGI ICIN 6 YAZDIK CUNKU COUNT 0
  LOOP
    IF VAR1.EXISTS(I) THEN
      PRINT('DEGER: ' || VAR1(I));
    ELSE
      PRINT('DEGER YOK.');
    END IF;
  END LOOP;
END;
