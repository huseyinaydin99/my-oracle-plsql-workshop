DESC REGIONS;
SELECT * FROM REGIONS;

CREATE SEQUENCE TEST_SEQ
START WITH 5
INCREMENT BY 1;

--TEST_SEQ.NEXTVAL KULLANIMI 19C ICIN KULLANILABLIR
--ANCAK BAZI SURUMLER ICIN KULLANILAMAZ.
--TEST_SEQ.NEXTVAL BU KULLANIMI PERFORMANSLI DEGILDIR

SELECT TEST_SEQ.NEXTVAL FROM DUAL;
SELECT TEST_SEQ.CURRVAL FROM DUAL;

DECLARE
  SEQ_NUM NUMBER;
BEGIN
  SEQ_NUM := TEST_SEQ.NEXTVAL; --<< BU KULLANIM TUM VERSIONLARDA GECERLIDIR.
  INSERT
  INTO REGIONS(REGION_ID, REGION_NAME)
  VALUES(SEQ_NUM, 'AFRIKA');
  SEQ_NUM := TEST_SEQ.CURRVAL;
  UPDATE REGIONS
  SET REGION_NAME = 'ANTARTIKA'
  WHERE REGION_ID = SEQ_NUM;
  -- ROLLBACK;
END;
