SELECT XMLFOREST(
    DEPT_T(DEPTNO,
    DNAME,
    CAST(MULTISET(
        SELECT EMPNO, ENAME
        FROM EMP E
        WHERE E.DEPTNO = D.DEPTNO
    ) AS EMPLIST_T))
    AS "BOLUM"
) AS DEPTXML
FROM DEPT D
WHERE DEPTNO = 20;