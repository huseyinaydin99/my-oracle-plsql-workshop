SELECT SYS_XMLAGG(SYS_XMLGEN(ENAME))
FROM EMP;

SELECT SYS_XMLGEN(ENAME)
FROM EMP;

SELECT XMLELEMENT("TARIH", HIREDATE)
FROM EMP; --ETIKETE TARIH ISMI VERIYOR.

SELECT XMLELEMENT("EMP",
 XMLELEMENT("ISMI", ENAME),
 XMLELEMENT("BASTAR", HIREDATE))
 AS SONUC
FROM EMP;



